package game;

// yang ada di interface itu abstract

public interface Fireball {
	
	//default = public static final (attribute)
	Integer attack = 10;
	
	
	//default = public abstract (method)
	void fireballRaid();
	void fireballAttack();
}
